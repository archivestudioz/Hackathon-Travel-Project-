.env
.env.local
web/config.js

# Protomaps offline extracts — fetched by scripts/fetch_tiles.sh, too big for git
web/tiles/*.pmtiles

# Generated ElevenLabs audio before it is uploaded to Supabase Storage
build/audio/

__pycache__/
*.py[cod]
.venv/
venv/
node_modules/
.DS_Store

# Downloaded source video. Never committed, never deployed, never rehosted —
# only the post URLs are used, via official oEmbed. See scripts/attach_media.py
*.mp4
*.mov
*.webm
*.mkv
clips/
videos/
downloads/
media.csv
